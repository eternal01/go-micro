syntax = "proto3";

package information;
option go_package="./information";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Information {
  rpc Ping(Request) returns(Response);
}
